import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    height: "100%",
    gap: 10,
  },
  header: {
    alignItems: "center",
    justifyContent: "space-between",
  },
  headerText: {
    flex: 1,
  },
});
