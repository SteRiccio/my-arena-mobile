import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    margin: 10,
    paddingHorizontal: 10,
    paddingVertical: 10,
    borderRadius: 5,
    borderWidth: 1,
    // alignItems: "center",
  },
  legend: {
    position: "absolute",
    top: -14,
    left: 10,
    paddingHorizontal: 6,
    fontWeight: "bold",
    fontSize: 16,
  },
});
