import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    marginTop: 16,
    paddingHorizontal: 10,
    paddingVertical: 10,
    borderRadius: 5,
    borderWidth: 1,
  },
  legend: {
    position: "absolute",
    top: -14,
    left: 10,
    paddingHorizontal: 6,
    fontSize: 16,
  },
});
