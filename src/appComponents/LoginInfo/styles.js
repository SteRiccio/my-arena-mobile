import { StyleSheet } from "react-native";

export default StyleSheet.create({
  notLoggedInContainer: {
    height: 100,
    justifyContent: "center",
  },
  userSummary: {
    maxWidth: "90%",
    minWidth: 250,
  },
  notLoggedInInnerContainer: {
    gap: 10,
  },
  notLoggedInfoContainer: {
    alignItems: "center",
  },
});
