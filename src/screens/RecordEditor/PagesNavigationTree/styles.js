import { StyleSheet } from "react-native";

export default StyleSheet.create({
  entityButtonContent: {
    flex: 1,
    alignItems: "baseline",
    justifyContent: "space-between",
  },
  entityButtonText: {
    fontSize: 20,
    fontWeight: "normal",
  },
  currentEntityButtonText: {
    fontSize: 24,
    fontWeight: "bold",
  },
});
