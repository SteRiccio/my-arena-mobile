import { StyleSheet } from "react-native";

export default StyleSheet.create({
  entityButton: {
    fontSize: 20,
    fontWeight: "normal",
  },
  entityButtonCurrentEntity: {
    fontSize: 24,
    fontWeight: "bold",
  },
});
